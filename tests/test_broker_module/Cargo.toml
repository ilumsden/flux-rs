[package]
name = "flux-core-rs-test-broker-module"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
flux-core = { path = "../../" }
clap = { version = "4.5", features = ["derive"] }
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
crate-type = ["cdylib"]
