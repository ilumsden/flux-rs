[package]
name = "flux-core-rs-test-rpc"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
flux-core = { path = "../../" }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
